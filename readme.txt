The folder DifferentialExpression contains all the information needed for the differential expression part of my PhD project (in theory).


Script

The folder "script" includes the bach script that will (in theory) run the pipeline until the normalized counts. 

 - The "experiments.txt" is the file with the list of experiments that are going to be run in that particular run.
 - The "samples.txt" is the file with the list of individual samples that are going to be run in that particular run.


 ** The script will run with one or the other. This is still to be done


Experiments

This in an output folder from the script for the counts. This will store the experiments and all the stuff that rn I dont know what is going to be